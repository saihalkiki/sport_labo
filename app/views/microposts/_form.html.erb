<%= form_with(model: micropost, local: true) do |form| %>
  <% if micropost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(micropost.errors.count, "error") %> prohibited this micropost from being saved:</h2>
      <ul>
      <% micropost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  競技：<%= form.collection_select(:sport_id, Sport.all, :id, :name,
                               { class: "form-control select2-search" }) %>

  <div class="field">
    <%= form.label :content,'募集要項' %>
    </br>
    <%= form.text_area :content %>
  </div>

開催エリア：<%= form.collection_select(:prefecture_id, Prefecture.all, :id, :name,
                               { class: "form-control select2-search" }) %>



  <div class="actions">
    <%= form.submit "募集を投稿する", class: "mt-3 btn btn-primary" %>
  </div>
<% end %>
